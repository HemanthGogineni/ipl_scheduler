package com.ipl.scheduler.util;

import com.ipl.scheduler.models.IPLMatch;

import java.util.Comparator;

public class ScheduleComparator implements Comparator<IPLMatch> {

    @Override
    public int compare(IPLMatch match, IPLMatch t1) {
        return 0;
    }
}
